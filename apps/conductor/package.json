{
  "name": "@conductor/app",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist --target bun",
    "dev": "bun run --watch src/index.ts",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "check": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "js-yaml": "^4.1.1",
    "liquidjs": "^10.25.0"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^25.4.0",
    "bun-types": "^1.3.10"
  }
}
